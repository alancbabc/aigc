{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MVShotPlan",
  "description": "Shot-level breakdown plan for imagery-type MV. Splits each segment into executable shots with time, composition, camera, motion, and generation notes. Instrumental segments get ambient_hold entries.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "source_segment_interpretation_ref", "shot_generation_rules", "shots"],
  "properties": {
    "schema_version": { "type": "string", "const": "1.0" },
    "song_title": { "type": "string", "minLength": 1 },
    "artist": { "type": "string" },
    "source_segment_interpretation_ref": { "type": "string", "minLength": 1 },
    "shot_generation_rules": {
      "type": "object",
      "additionalProperties": false,
      "required": ["instrumental_hold_policy", "long_segment_policy", "video_max_duration_seconds"],
      "properties": {
        "instrumental_hold_policy": { "type": "string", "enum": ["reuse_previous_visual_or_create_ambient_hold"] },
        "long_segment_policy": { "type": "string", "enum": ["split_before_shot_planning_if_over_40_seconds"] },
        "video_max_duration_seconds": { "type": "number", "minimum": 1.0 },
        "over_max_policy": { "type": "string" }
      }
    },
    "shots": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "shot_id", "parent_segment_id", "parent_section_id",
          "time_range", "shot_type", "shot_role",
          "literal_meaning_zh", "deep_meaning", "static_frame_description",
          "key_imagery", "composition", "shot_direction", "generation_notes"
        ],
        "properties": {
          "shot_id": { "type": "string", "minLength": 1 },
          "parent_segment_id": { "type": "string", "minLength": 1 },
          "parent_section_id": { "type": "string", "minLength": 1 },
          "time_range": {
            "type": "object",
            "additionalProperties": false,
            "required": ["start_time", "end_time", "duration_seconds"],
            "properties": {
              "start_time": { "type": "number", "minimum": 0 },
              "end_time": { "type": "number", "minimum": 0 },
              "duration_seconds": { "type": "number", "minimum": 0 }
            }
          },
          "lyric_refs": {
            "type": "array",
            "items": { "type": "string" }
          },
          "shot_type": {
            "type": "string",
            "enum": ["lyric_imagery", "singer_performance"],
            "description": "lyric_imagery: lyrics have concrete visual entities. singer_performance: no entities, use singer silhouette."
          },
          "shot_role": {
            "type": "string",
            "enum": [
              "establishing_image", "symbolic_detail", "emotional_peak",
              "transition_image", "motif_development", "ambient_hold",
              "memory_echo", "climax_image", "resolution_image"
            ]
          },
          "literal_meaning_zh": { "type": "string", "minLength": 1 },
          "deep_meaning": { "type": "string", "minLength": 1, "description": "What the lyrics truly express at a deeper level." },
          "static_frame_description": {
            "type": "string", "minLength": 1,
            "description": "Concrete static image description: what is in the frame, shot size, entity positions, spatial relationships. Actionable for an image model."
          },
          "key_imagery": {
            "type": "array",
            "minItems": 0,
            "items": { "type": "string" },
            "description": "Only concrete entities from the lyrics. Empty if no explicit visual entities exist."
          },
          "composition": {
            "type": "object",
            "additionalProperties": false,
            "required": ["shot_size", "camera_angle", "foreground", "midground", "background", "focal_point", "depth"],
            "properties": {
              "shot_size": {
                "type": "string",
                "enum": ["extreme_wide_shot", "wide_shot", "medium_wide_shot", "medium_shot", "medium_close_up", "close_up", "extreme_close_up"]
              },
              "camera_angle": {
                "type": "string",
                "enum": ["eye_level", "low_angle", "slightly_low_angle", "high_angle", "slightly_high_angle", "dutch_angle", "overhead"]
              },
              "foreground": { "type": "string" },
              "midground": { "type": "string" },
              "background": { "type": "string" },
              "focal_point": { "type": "string" },
              "depth": {
                "type": "string",
                "enum": ["shallow_space", "medium_space", "deep_space", "flat_space"]
              }
            }
          },
          "shot_direction": {
            "type": "object",
            "additionalProperties": false,
            "description": "Combined camera + motion + transition for the coming shot-direction refinement stage.",
            "required": ["camera_motion", "movement_speed", "lens_feeling", "stability", "subject_motion", "environment_motion", "motion_intensity", "transition_in", "transition_out", "transition_duration"],
            "properties": {
              "camera_motion": {
                "type": "string",
                "enum": ["static", "slow_push_in", "slow_pull_out", "slow_pan_left", "slow_pan_right", "slow_tilt_up", "slow_tilt_down", "slow_dolly", "slow_tracking", "slow_zoom_in", "slow_zoom_out", "floating_drift", "none_or_minimal_drift"]
              },
              "movement_speed": { "type": "string", "enum": ["very_slow", "slow", "moderate"] },
              "lens_feeling": { "type": "string", "enum": ["wide_cinematic", "standard", "telephoto_compressed", "macro_dreamlike"] },
              "stability": { "type": "string", "enum": ["locked", "floating", "handheld_subtle", "drifting"] },
              "subject_motion": { "type": "string" },
              "environment_motion": { "type": "string" },
              "motion_intensity": { "type": "string", "enum": ["very_low", "low", "medium", "high"] },
              "transition_in": { "type": "string" },
              "transition_out": { "type": "string" },
              "transition_duration": { "type": "number", "minimum": 0 }
            }
          },
          "visual_style": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "scene_type": { "type": "string" },
              "color_palette": { "type": "array", "items": { "type": "string" } },
              "lighting": { "type": "string" },
              "texture": { "type": "string" }
            }
          },
          "emotion": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "primary": { "type": "string" },
              "secondary": { "type": "array", "items": { "type": "string" } },
              "intensity": { "type": "number", "minimum": 0.0, "maximum": 1.0 }
            }
          },
          "generation_notes": {
            "type": "object",
            "additionalProperties": false,
            "required": ["image_prompt_focus", "video_prompt_focus", "avoid"],
            "properties": {
              "generate_new_image": { "type": "boolean" },
              "reuse_from_shot_id": { "type": "string" },
              "image_prompt_focus": { "type": "string", "minLength": 1 },
              "video_prompt_focus": { "type": "string", "minLength": 1 },
              "avoid": { "type": "array", "items": { "type": "string" } }
            }
          }
        }
      }
    }
  }
}
